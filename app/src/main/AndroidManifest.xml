<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.zup.simcityrickandmorty">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_rick"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_rick"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimCityRickAndMorty"
        tools:targetApi="31">
        <activity
            android:name=".CharactersListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.splash.view.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>